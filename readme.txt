PPS_final.py : includes the final version of Preprocessing (Remove stopwords, Keywords selection,  Stemming, and Indexing.)

microblog_final: 
includes the running server configuration, 
the processed data: tweets files (e.g., inputList1.pickle), index files (e.g., index1.py), IDF file, clustering results files (e.g., filecluster1.pickle),  GUI files (e.g. clustering.html)
and some function modules (the prediction.py, the fullTextSearch.py, etc.)

prediction: includes the code for calculating the document frequency and predicting and also the prediction results. 

LanguageDectector.py: includes the code for language identification. 

cluster: includes the codes of all the algorithms and distance measurement we have tried.